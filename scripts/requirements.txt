# Hermes relay (scripts/relay.py) Python 의존성
# 설치:  python3 -m venv .venv && .venv/bin/pip install -r scripts/requirements.txt
# 실행:  .venv/bin/python scripts/relay.py
supabase==2.31.0
python-dotenv==1.2.1
requests==2.32.5
