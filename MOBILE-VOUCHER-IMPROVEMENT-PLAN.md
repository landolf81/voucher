# 모바일 교환권 시스템 개선 계획

## 현재 문제점

### 1. URL 구조 문제
- **현재**: `https://voucher-iota.vercel.app/mobile/vouchers/Yf2xnJ1T14sjzVpUNNOYEXBE1jh90V_mh4k6563`
- **문제점**:
  - 토큰이 너무 길고 복잡함 (43자)
  - 사용자 친화적이지 않음
  - 공유하기 어려움

### 2. UI/UX 문제
- **현재 상태**:
  - 정보가 너무 많이 나열됨 (성명, 회원번호, 생년월일, 연락처, QR코드, 일련번호 등)
  - 모바일 화면에서 스크롤이 많이 필요
  - 중요 정보(QR코드, 금액)가 분산되어 있음

## 개선 방안

### 1. URL 단축화 ⭐ 최우선
```
현재: /mobile/vouchers/Yf2xnJ1T14sjzVpUNNOYEXBE1jh90V_mh4k6563 (43자)
개선안: /v/A1B2C3 (8-10자 Short ID)
```

**구현 방법**:
- Short URL 시스템 도입
- 6-8자 단축 ID 생성 (Base62: 0-9, a-z, A-Z)
- 데이터베이스에 매핑 테이블 추가
- 예: `https://voucher-iota.vercel.app/v/A1B2C3`

**장점**:
- 공유하기 쉬움
- QR 코드 크기 감소
- 타이핑 가능
- 기억하기 쉬움

### 2. UI 단순화 ⭐

#### 옵션 A: 미니멀 디자인 (추천)
```
┌─────────────────────────┐
│   [교환권명]            │
│                         │
│   ┏━━━━━━━━━━━━━┓      │
│   ┃             ┃      │
│   ┃   QR CODE   ┃      │
│   ┃             ┃      │
│   ┗━━━━━━━━━━━━━┛      │
│                         │
│   100,000원             │
│                         │
│   [사용 가능]           │
│                         │
│   일련번호: 123456      │
│   발행일: 2025.01.01    │
└─────────────────────────┘
```

**특징**:
- QR 코드 중심
- 개인정보 최소화 (성명, 생년월일, 연락처 제거)
- 필수 정보만 표시 (금액, 상태, 일련번호)
- 한 화면에 모든 정보 표시

#### 옵션 B: 카드 슬라이더 (여러 교환권용)
```
┌─────────────────────────┐
│ ← 교환권 1/3 →          │
│                         │
│   [슬라이드 카드]       │
│   - QR 코드             │
│   - 금액                │
│   - 상태                │
└─────────────────────────┘
```

### 3. 구체적 개선 사항

#### 제거할 정보
- ❌ 성명 (개인정보 노출)
- ❌ 회원번호 (불필요)
- ❌ 생년월일 (개인정보 노출)
- ❌ 연락처 (개인정보 노출)
- ❌ 조합명 (헤더에 한 번만)

#### 유지할 정보
- ✅ QR 코드 (크게 표시)
- ✅ 금액 (크고 명확하게)
- ✅ 상태 (사용 가능/사용 완료)
- ✅ 일련번호 (작게)
- ✅ 발행일 (작게)

### 4. 디자인 개선

#### 색상 체계
```css
배경: 그라데이션 (#f0f9ff → #e0f2fe)
카드: 흰색 + 그림자
금액: 큰 글씨, 진한 파랑 (#1e40af)
QR 코드: 중앙 정렬, 큰 크기 (250px)
상태: 녹색(사용 가능) / 회색(사용 완료)
```

#### 레이아웃
- QR 코드를 화면 중앙 상단에 크게
- 금액을 QR 코드 아래 바로 표시
- 나머지 정보는 하단에 작게
- 여백 충분히

### 5. 추가 기능 제안

#### A. 다크 모드
- 시스템 설정 따라가기
- 밤에 사용 시 눈 편함

#### B. 교환권 저장
- "휴대폰에 저장" 버튼
- Apple Wallet / Google Pay 연동
- PWA 설치 유도

#### C. 공유 기능
- 단축 URL 복사 버튼
- 카카오톡, 문자 공유

## 구현 우선순위

### Phase 1: 즉시 가능 (1-2시간)
1. ✅ UI 단순화 (개인정보 제거)
2. ✅ QR 코드 크기 확대
3. ✅ 레이아웃 재구성

### Phase 2: 단기 (1일)
1. ⭐ Short URL 시스템 구축
2. ✅ 다크 모드 추가
3. ✅ 공유 기능 추가

### Phase 3: 중기 (2-3일)
1. ⚡ Apple Wallet / Google Pay 연동
2. ⚡ PWA 기능 강화
3. ⚡ 애니메이션 추가

## 기술 스택

### Short URL
```typescript
// 새 테이블: short_urls
{
  id: string (UUID)
  short_code: string (6-8자, unique index)
  original_token: string (기존 긴 토큰)
  created_at: timestamp
  expires_at: timestamp
  access_count: number
}
```

### Base62 인코딩
```typescript
function generateShortCode(): string {
  const chars = '0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ';
  let code = '';
  for (let i = 0; i < 6; i++) {
    code += chars[Math.floor(Math.random() * chars.length)];
  }
  return code;
}
```

## 예상 효과

### URL 개선
- 길이: 43자 → 6자 (85% 감소)
- 공유 편의성: ⭐⭐⭐⭐⭐
- 보안: 동일 (short code도 예측 불가능)

### UI 개선
- 로딩 속도: 개인정보 없어져서 더 빠름
- 사용자 경험: 한 눈에 정보 파악
- 개인정보 보호: 민감 정보 미표시

## 다음 단계

1. **즉시 실행 가능**: UI 단순화 (코드 수정만)
2. **DB 마이그레이션 필요**: Short URL 시스템
3. **외부 서비스 연동**: Wallet 기능

어떤 것부터 시작하시겠습니까?
- Option 1: UI만 먼저 개선 (빠름, 영향 크지 않음)
- Option 2: Short URL 먼저 구축 (시간 걸림, 근본적 해결)
- Option 3: 둘 다 동시 진행 (추천)
